# プロジェクト開発規約・セキュリティ絶対遵守ルール

## 【最重要・絶対禁止事項】原本テスト画像・教材メディアのコミット禁止
1. **テスト原本画像・スキャン用紙・著作権教材のアップロード絶対禁止**:
   - ユーザーから提供されたテスト原本画像（例: `*整数*.png`, `*テスト*.png`, `*第*回*.png` 等）、教材のスキャン、個人情報、機密データは、**いかなる場合もGitリポジトリ（特にパブリック）にステージング・コミット・プッシュしてはならない**。
   - これらの画像は「コード生成・類題設計のためのローカル参照」にのみ使用すること。
2. **Git管理の事前検証義務**:
   - `git add` やコミットを実行する前に、必ず `git status` を確認し、画像ファイル（`.png`, `.jpg`, `.jpeg`, `.pdf` 等）が含まれていないことを徹底検証すること。
   - 原本画像や教材関連ファイルは必ず `.gitignore` に登録して除外すること。
3. **類題エンジンの数値独立性**:
   - 原本の数値をそのまま本番出題コードにハードコードせず、必ず類題生成ロジックによって新しい数値を生成すること。
